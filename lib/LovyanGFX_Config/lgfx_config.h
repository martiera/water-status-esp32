#pragma once

// Disable all Asian font includes to prevent compilation issues
#define LGFX_USE_V1
#define LGFX_AUTODETECT

// Explicitly disable font features we don't need
#undef LGFX_INCLUDE_ALL_FONTS
